# Claude Notes

## Build Commands

- Use `bun` instead of `npm` for all commands
- Build: `bun run build`
- Dev: `bun dev`
- Test: `bun test`
- Lint: `bun lint`
- Lint with Auto Fix: `bun lint:fix`
- Format: `bun fmt`
